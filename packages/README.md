# Public packages

Status: experimental
Content: informative
Canonical source: this file
Generated: no

`packages/` contains thin public validators, clients, and authoring libraries justified by a stable public contract.

Packages MUST operate without private source or services and MUST NOT embed production orchestration, policy, privileged execution, host topology, credentials, or operator configuration. The monorepo uses one coordinated package release version initially.
